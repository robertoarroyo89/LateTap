"use client";
import { useCallback, useEffect, useState } from "react";
import type { Locale } from "@/config/app";
import { formatPrice, formatSlotDate, localizedServiceName } from "@/lib/format";
import type { Reservation } from "@/types/domain";
export function BookingsList({ locale, business = false }: { locale: Locale; business?: boolean }) { const [items, setItems] = useState<Reservation[]>([]); const [error, setError] = useState(false); const load = useCallback(() => fetch(business ? "/api/v1/business/reservations" : "/api/v1/me/reservations").then(async (response) => { if (!response.ok) throw new Error(); setItems((await response.json()).data.items); }).catch(() => setError(true)), [business]); useEffect(() => { void load(); }, [load]); const action = async (item: Reservation, type: string) => { const url = business ? `/api/v1/business/reservations/${item.id}` : `/api/v1/reservations/${item.id}/cancel`; await fetch(url, { method: business ? "PATCH" : "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(business ? { action: type } : {}) }); void load(); }; return <div className="bookings-list">{error && <div className="dashboard-notice">{locale === "es" ? "Inicia sesión y configura Firebase para ver reservas." : "Sign in and configure Firebase to see bookings."}</div>}{items.map((item) => <article key={item.id}><div className="booking-date"><span>{new Date(item.startAt).getDate()}</span><small>{new Intl.DateTimeFormat(locale, { month: "short" }).format(new Date(item.startAt))}</small></div><div><strong>{localizedServiceName(item.serviceSnapshot, locale)}</strong><span>{item.businessSnapshot.name} · {formatSlotDate(item.startAt, locale, item.timezone)}</span></div><b>{formatPrice(item.priceCents, item.currency, locale)}</b><span className={`status-badge ${item.status}`}>{item.status.replaceAll("_", " ")}</span>{item.status === "confirmed" && (business ? <div className="row-actions"><button onClick={() => action(item, "complete")}>Complete</button><button onClick={() => action(item, "no_show")}>No-show</button><button onClick={() => action(item, "cancel")}>Cancel</button></div> : <button onClick={() => action(item, "cancel")}>{locale === "es" ? "Cancelar" : "Cancel"}</button>)}</article>)}{!error && !items.length && <div className="dashboard-notice">{locale === "es" ? "Todavía no hay reservas." : "No bookings yet."}</div>}</div>; }
