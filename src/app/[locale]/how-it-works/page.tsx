import { notFound } from "next/navigation";
import { isLocale } from "@/config/app";
export default async function Page({ params }: PageProps<"/[locale]/how-it-works">) { const { locale } = await params; if (!isLocale(locale)) notFound(); const es = locale === "es"; return <div className="simple-page shell"><p className="kicker">HOW IT WORKS</p><h1>{es ? "De última hora. Justo a tiempo." : "Last minute. Right on time."}</h1><div className="how-grid">{(es ? [["1","Encuentra","Descubre citas que acaban de quedar libres."],["2","Reserva","Confirma en segundos desde tu móvil."],["3","Disfruta","Acude al establecimiento y paga allí."]] : [["1","Find","Discover appointments that just opened up."],["2","Book","Confirm in seconds from your phone."],["3","Enjoy","Go to the venue and pay there."]]).map(([number,title,body]) => <article key={number}><span>{number}</span><h2>{title}</h2><p>{body}</p></article>)}</div></div>; }
