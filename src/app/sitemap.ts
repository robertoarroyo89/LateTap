import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:3000"; const routes = ["", "/explore", "/for-businesses", "/how-it-works", "/legal", "/privacy", "/cookies", "/terms"]; return ["es", "en"].flatMap((locale) => routes.map((route) => ({ url: `${base}/${locale}${route}`, lastModified: new Date(), changeFrequency: route === "/explore" ? "hourly" as const : "weekly" as const, priority: route === "" ? 1 : .7, alternates: { languages: { es: `${base}/es${route}`, en: `${base}/en${route}` } } }))); }
